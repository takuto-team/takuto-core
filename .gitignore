/target
.DS_Store
.claude/

# Local workflow snapshot / Maestro runtime under configured repo_path (not for version control)
.maestro/

config.toml
.env
maestro.env

# Local issue tracking files
/issues

# Optional per-developer Claude/Cursor skills merged into container home on start (see README).
/skills
