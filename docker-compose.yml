services:
  maestro:
    build: .
    ports:
      - "8080:8080"
    cap_add:
      - NET_ADMIN
    volumes:
      # Configuration file
      - ./config.toml:/etc/maestro/config.toml:ro
      # Custom environment variables (optional)
      - ./maestro.env:/etc/maestro/env:ro
      # Persistent auth state — mounted as root, copied to maestro user at startup
      - claude-auth:/root/.claude
      - gh-auth:/root/.config/gh
      - acli-auth:/root/.config/acli
      # Workspace — cloned during setup, persisted across restarts
      - workspace:/workspace
      # npm cache — mounted as root, copied to maestro user at startup
      - npm-cache:/root/.npm
    environment:
      # Figma API token (optional, also configurable in config.toml)
      - FIGMA_API_TOKEN=${FIGMA_API_TOKEN:-}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  claude-auth:
  gh-auth:
  acli-auth:
  workspace:
  npm-cache:
