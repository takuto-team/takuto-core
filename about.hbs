# Third-party Rust crates — Takuto Core

The crates below are statically linked into the `takuto` binary and redistributed in the
image, each under its own license. Generated with `cargo about generate about.hbs`. Takuto
Core's own source is FSL-1.1-ALv2 (see LICENSE); these are its dependencies.

## Licenses in use

{{#each overview}}
- {{name}}: {{count}}
{{/each}}

## Crates and full license texts

{{#each licenses}}
### {{name}}

Used by: {{#each used_by}}{{crate.name}} {{crate.version}}{{#unless @last}}, {{/unless}}{{/each}}

<details><summary>License text</summary>

```text
{{{text}}}
```

</details>

{{/each}}
